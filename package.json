{
  "name": "wangyun.fan",
  "version": "1.0.0",
  "private": true,
  "description": "Personal homepage for wangyun.fan",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 8787",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 8787",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "astro": "^5.18.1"
  }
}
