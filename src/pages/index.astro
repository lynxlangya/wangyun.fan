<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>王雲帆 · Wang Yunfan</title>
<link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
<link rel="alternate icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Crect width='32' height='32' rx='7' fill='%23fafaf7'/%3E%3Crect x='12.5' y='9' width='3.2' height='13' fill='%230e0e0c'/%3E%3Ccircle cx='21.5' cy='21' r='1.7' fill='%231d4ed8'/%3E%3C/svg%3E">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@300;400;500;600&family=IBM+Plex+Serif:ital,wght@0,400;1,400&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:        #fafaf7;
    --bg-2:      #f3f1ea;
    --ink:       #0e0e0c;
    --ink-soft:  #2a2a26;
    --muted:     #8a8a82;
    --muted-2:   #b6b5ad;
    --rule:      #e5e3dc;
    --accent:    #1d4ed8;
    --accent-soft:#dbe4fb;

    --mono: "IBM Plex Mono", ui-monospace, "SFMono-Regular", Menlo, Consolas,
            "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", monospace;
    --serif: "IBM Plex Serif", "Songti SC", "STSong", Georgia, serif;
  }

  *{box-sizing:border-box;margin:0;padding:0}
  html{-webkit-text-size-adjust:100%}
  body{
    background:var(--bg);
    color:var(--ink);
    font-family:var(--mono);
    font-size:14px;
    line-height:1.65;
    font-feature-settings:"ss01","ss02","calt","liga";
    -webkit-font-smoothing:antialiased;
    text-rendering:optimizeLegibility;
    letter-spacing:-0.005em;
  }
  ::selection{background:var(--accent);color:#fff}

  /* subtle paper grain */
  body::before{
    content:"";
    position:fixed;inset:0;pointer-events:none;z-index:0;
    background-image:
      radial-gradient(rgba(0,0,0,.018) 1px, transparent 1px);
    background-size: 3px 3px;
    mix-blend-mode:multiply;
  }

  main{
    position:relative;z-index:1;
    max-width:680px;
    padding:96px 32px 160px;
    margin:0 auto;
  }
  @media (max-width:520px){
    main{padding:64px 22px 120px}
    body{font-size:13.5px}
  }

  /* ---------- stagger entry ---------- */
  .r{opacity:0;transform:translateY(8px);
     animation: rise .9s cubic-bezier(.2,.6,.2,1) forwards}
  @keyframes rise{to{opacity:1;transform:none}}
  .d1{animation-delay:.05s} .d2{animation-delay:.16s}
  .d3{animation-delay:.28s} .d4{animation-delay:.42s}
  .d5{animation-delay:.56s} .d6{animation-delay:.70s}
  .d7{animation-delay:.84s} .d8{animation-delay:.98s}
  .d9{animation-delay:1.12s} .d10{animation-delay:1.26s}
  @media (prefers-reduced-motion: reduce){
    .r{opacity:1;transform:none;animation:none}
  }

  /* ---------- header ---------- */
  header{
    display:flex;align-items:flex-start;justify-content:space-between;
    gap:24px;
  }
  .id{
    display:flex;flex-direction:column;gap:6px;
  }
  .name-row{
    display:flex;align-items:baseline;gap:14px;flex-wrap:wrap;
  }
  h1{
    font-family:var(--mono);
    font-weight:500;
    font-size:17px;
    letter-spacing:-0.01em;
    color:var(--ink);
  }
  h1 .en{
    color:var(--muted);
    font-weight:400;
    margin-left:2px;
  }
  .caret{
    display:inline-block;width:7px;height:14px;
    background:var(--ink);
    transform:translateY(2px);
    margin-left:2px;
    animation:blink 1.05s steps(1) infinite;
  }
  @keyframes blink{50%{opacity:0}}

  .role{
    color:var(--muted);
    font-size:13px;
    letter-spacing:.01em;
  }
  .role span{color:var(--ink-soft)}

  /* status */
  .status{
    display:flex;align-items:center;gap:8px;
    font-size:12px;color:var(--muted);
    padding:4px 0 0;
    cursor:pointer;
    background:none;
    border:none;
    font-family:inherit;
    letter-spacing:inherit;
    -webkit-appearance:none;
    appearance:none;
  }
  .dot{
    position:relative;width:7px;height:7px;border-radius:50%;
    background:var(--accent);
    box-shadow:0 0 0 0 rgba(29,78,216,.45);
    animation: pulse 2.4s ease-out infinite;
  }
  @keyframes pulse{
    0%{box-shadow:0 0 0 0 rgba(29,78,216,.45)}
    80%,100%{box-shadow:0 0 0 8px rgba(29,78,216,0)}
  }
  .status-text{transition: color .2s ease}
  .status:hover .status-text{color:var(--ink)}

  /* ---------- intro ---------- */
  .intro{
    margin-top:48px;
    font-size:15px;
    line-height:1.75;
    color:var(--ink-soft);
    max-width:62ch;
  }
  .intro p + p{margin-top:1em}
  .intro .zh{color:var(--ink)}
  .intro em{font-family:var(--serif);font-style:italic;color:var(--ink)}

  /* ---------- section ---------- */
  section{margin-top:64px}
  .sec-head{
    display:flex;align-items:baseline;justify-content:space-between;
    padding-bottom:10px;
    border-bottom:1px solid var(--rule);
    margin-bottom:18px;
  }
  .sec-head h2{
    font-size:11px;
    font-weight:500;
    letter-spacing:.18em;
    text-transform:uppercase;
    color:var(--ink);
  }
  .sec-head h2 .zh{
    color:var(--muted);
    margin-left:10px;
    letter-spacing:.06em;
  }
  .sec-head .meta{
    font-size:11px;color:var(--muted-2);
    letter-spacing:.06em;
    white-space:nowrap;
  }

  /* ---------- now list ---------- */
  .now{display:flex;flex-direction:column;gap:6px;font-size:14px}
  .now li{
    list-style:none;
    display:grid;grid-template-columns:18px 1fr;
    gap:4px;
    color:var(--ink-soft);
  }
  .now .mk{color:var(--muted-2)}
  .now strong{color:var(--ink);font-weight:500}

  /* ---------- entry rows (work / invest / writing) ---------- */
  .rows{display:flex;flex-direction:column}
  .row{
    display:grid;
    grid-template-columns: 92px 1fr auto;
    gap:18px;
    padding:14px 0;
    border-bottom:1px dashed var(--rule);
    align-items:baseline;
    transition: background .25s ease, padding .25s ease;
  }
  .row:last-child{border-bottom:none}
  .row .when{
    color:var(--muted);font-size:12px;
    letter-spacing:.02em;
    padding-top:1px;
  }
  .row .body{display:flex;flex-direction:column;gap:3px}
  .row .ttl{color:var(--ink);font-weight:500;font-size:14px}
  .row .ttl .at{color:var(--muted);font-weight:400}
  .row .desc{color:var(--muted);font-size:12.5px;line-height:1.55}
  .row .tag{
    font-size:10.5px;
    color:var(--muted-2);
    letter-spacing:.1em;
    text-transform:uppercase;
    padding-top:2px;
    white-space:nowrap;
  }
  .row:hover{
    background:linear-gradient(90deg, transparent, var(--bg-2) 6%, var(--bg-2) 94%, transparent);
    padding-left:8px;padding-right:8px;
    margin-left:-8px;margin-right:-8px;
  }
  @media (max-width:520px){
    .row{grid-template-columns: 76px 1fr;gap:14px}
    .row .tag{grid-column:1 / -1;padding-top:2px}
  }

  /* ---------- writing (with read-time hover) ---------- */
  .write .row .tag{color:var(--muted)}
  .write .row:hover .tag{color:var(--accent)}

  /* ---------- elsewhere ---------- */
  .elsewhere{
    display:grid;
    grid-template-columns:repeat(2, 1fr);
    gap:2px 28px;
  }
  @media (max-width:520px){.elsewhere{grid-template-columns:1fr}}
  .elsewhere a{
    display:flex;justify-content:space-between;align-items:baseline;
    padding:11px 0;
    border-bottom:1px dashed var(--rule);
    color:var(--ink);
    text-decoration:none;
    font-size:13.5px;
    position:relative;
  }
  .elsewhere a .k{color:var(--muted);font-size:11px;letter-spacing:.1em;text-transform:uppercase}
  .elsewhere a .v{
    color:var(--ink-soft);
    transition: color .2s ease, transform .25s cubic-bezier(.2,.6,.2,1);
  }
  .elsewhere a:hover .v{color:var(--accent);transform:translateX(-2px)}
  .elsewhere a::after{
    content:"↗";
    position:absolute;right:-2px;top:50%;transform:translate(-6px,-50%) rotate(0deg);
    color:var(--accent);
    opacity:0;
    transition: opacity .25s ease, transform .25s cubic-bezier(.2,.6,.2,1);
    font-size:12px;
  }
  .elsewhere a:hover::after{opacity:1;transform:translate(0,-50%) rotate(0deg)}

  /* generic link */
  a.lnk{
    color:var(--ink);
    text-decoration:none;
    background-image: linear-gradient(var(--accent), var(--accent));
    background-repeat:no-repeat;
    background-size: 0% 1px;
    background-position: 0 100%;
    transition: background-size .35s cubic-bezier(.2,.6,.2,1), color .2s;
    padding-bottom:1px;
  }
  a.lnk:hover{background-size:100% 1px;color:var(--accent)}

  /* ---------- footer ---------- */
  footer{
    margin-top:80px;
    padding-top:18px;
    border-top:1px solid var(--rule);
    display:flex;justify-content:space-between;align-items:baseline;
    color:var(--muted-2);
    font-size:11px;letter-spacing:.06em;
  }
  footer .sig{display:flex;gap:14px;align-items:baseline}
  footer kbd{
    font-family:var(--mono);
    font-size:10px;
    border:1px solid var(--rule);
    background:var(--bg);
    border-bottom-width:2px;
    padding:1px 5px;
    border-radius:3px;
    color:var(--muted);
  }

  /* color toast for ?: ::after caret on h1 link */
  .name-row .here{
    font-size:10.5px;color:var(--muted-2);
    letter-spacing:.1em;text-transform:uppercase;
    padding-left:2px;
  }

  /* tiny ascii brand mark */
  .mark{
    font-family:var(--mono);
    font-size:11px;color:var(--muted-2);
    letter-spacing:.04em;
    user-select:none;
  }

  /* easter-egg: click status to flip text */
  .status[data-flip="1"] .status-text::before{content:"available for coffee / "}
  .status[data-flip="1"] .status-text > .a{display:none}
  .status .b{display:none}
  .status[data-flip="1"] .a{display:none}
  .status[data-flip="1"] .b{display:inline}
</style>
</head>
<body>

<main>

  <!-- ===== HEADER ===== -->
  <header class="r d1">
    <div class="id">
      <div class="name-row">
        <h1>王雲帆 <span class="en">Wang Yunfan</span><span class="caret" aria-hidden="true"></span></h1>
      </div>
      <div class="role">
        <span>AI Engineer</span> · <span>Investor</span> · <span>Founder</span>
      </div>
    </div>

    <button class="status" id="status" aria-label="current status" type="button">
      <span class="dot" aria-hidden="true"></span>
      <span class="status-text">
        <span class="a">in SF · open to coffee</span>
        <span class="b">回复邮件中 · be back soon</span>
      </span>
    </button>
  </header>

  <!-- ===== INTRO ===== -->
  <div class="intro r d2">
    <p class="zh">
      在湾区做 AI。白天写代码、训模型、和 founder 聊；
      晚上读论文、写点想法、给早期项目写支票。
    </p>
    <p>
      Building &amp; backing tools at the edge of what models can do —
      from <em>inference systems</em> and <em>developer agents</em> to the
      odd consumer experiment. Previously shipped infra at scale; now
      mostly betting on small teams with strong taste.
    </p>
  </div>

  <!-- ===== NOW ===== -->
  <section class="r d3">
    <div class="sec-head">
      <h2>Now <span class="zh">现在</span></h2>
      <div class="meta">May 2026 · San Francisco</div>
    </div>
    <ul class="now">
      <li><span class="mk">—</span><span>Building <strong>a stealth AI infra company</strong>. 0→1 阶段，三人小队。</span></li>
      <li><span class="mk">—</span><span>Angel checks in <strong>dev tools, agents, voice</strong>. 写过的 SAFE 比写过的 PR 多。</span></li>
      <li><span class="mk">—</span><span>Reading: <em>The Beginning of Infinity</em>, again, slower this time.</span></li>
      <li><span class="mk">—</span><span>Running a weekly <strong>dim sum + demo day</strong> for SF builders — DM if you want in.</span></li>
    </ul>
  </section>

  <!-- ===== WORK ===== -->
  <section class="r d4">
    <div class="sec-head">
      <h2>Work <span class="zh">经历</span></h2>
      <div class="meta">2017 — present</div>
    </div>
    <div class="rows">
      <div class="row">
        <div class="when">2025 — now</div>
        <div class="body">
          <div class="ttl">Founder &amp; CTO <span class="at">/ Stealth</span></div>
          <div class="desc">Faster, cheaper inference for small models. We make the API call you don't notice.</div>
        </div>
        <div class="tag">Building</div>
      </div>
      <div class="row">
        <div class="when">2022 — 25</div>
        <div class="body">
          <div class="ttl">Staff Engineer <span class="at">/ a large model lab</span></div>
          <div class="desc">Trained pre-/post-training pipelines for a frontier model family. Shipped two generations.</div>
        </div>
        <div class="tag">ML Infra</div>
      </div>
      <div class="row">
        <div class="when">2020 — 22</div>
        <div class="body">
          <div class="ttl">Tech Lead <span class="at">/ search &amp; ranking, a consumer app</span></div>
          <div class="desc">Recsys for 200M+ DAU. Cut tail latency by half, doubled CTR on cold-start sessions.</div>
        </div>
        <div class="tag">Recsys</div>
      </div>
      <div class="row">
        <div class="when">2017 — 20</div>
        <div class="body">
          <div class="ttl">Software Engineer <span class="at">/ first job, big tech</span></div>
          <div class="desc">Distributed systems. Wrote my first 100k lines of Go and one regrettable Python script.</div>
        </div>
        <div class="tag">Systems</div>
      </div>
    </div>
  </section>

  <!-- ===== INVEST ===== -->
  <section class="r d5">
    <div class="sec-head">
      <h2>Invest <span class="zh">投资</span></h2>
      <div class="meta">Angel · pre-seed · ~30 checks</div>
    </div>
    <div class="rows">
      <div class="row">
        <div class="when">2025</div>
        <div class="body">
          <div class="ttl">Halcyon Labs</div>
          <div class="desc">Verifiable inference for high-stakes agents.</div>
        </div>
        <div class="tag">Seed · led</div>
      </div>
      <div class="row">
        <div class="when">2024</div>
        <div class="body">
          <div class="ttl">Pinecone-shaped thing</div>
          <div class="desc">Spatial memory for long-running coding agents.</div>
        </div>
        <div class="tag">Pre-seed</div>
      </div>
      <div class="row">
        <div class="when">2024</div>
        <div class="body">
          <div class="ttl">Mockingbird</div>
          <div class="desc">Real-time voice cloning for accessibility, on-device.</div>
        </div>
        <div class="tag">Pre-seed</div>
      </div>
      <div class="row">
        <div class="when">2023</div>
        <div class="body">
          <div class="ttl">Strata.dev</div>
          <div class="desc">A code review tool that actually understands the diff.</div>
        </div>
        <div class="tag">Seed</div>
      </div>
    </div>
  </section>

  <!-- ===== WRITING ===== -->
  <section class="r d6 write">
    <div class="sec-head">
      <h2>Writing <span class="zh">写作</span></h2>
      <div class="meta">essays · notes</div>
    </div>
    <div class="rows">
      <div class="row">
        <div class="when">2026.03</div>
        <div class="body">
          <div class="ttl"><a class="lnk" href="#">The hidden cost of cheap tokens</a></div>
          <div class="desc">为什么 token 单价下降，反而让产品工程变得更难。</div>
        </div>
        <div class="tag">12 min</div>
      </div>
      <div class="row">
        <div class="when">2026.01</div>
        <div class="body">
          <div class="ttl"><a class="lnk" href="#">小模型、慢思考、长上下文</a></div>
          <div class="desc">A field note on where I think the next year of inference goes.</div>
        </div>
        <div class="tag">8 min</div>
      </div>
      <div class="row">
        <div class="when">2025.11</div>
        <div class="body">
          <div class="ttl"><a class="lnk" href="#">How I read a startup in 20 minutes</a></div>
          <div class="desc">投资人和工程师在尽调时关心的，其实是同一件事。</div>
        </div>
        <div class="tag">6 min</div>
      </div>
      <div class="row">
        <div class="when">2025.07</div>
        <div class="body">
          <div class="ttl"><a class="lnk" href="#">Notes on taste, from a person who has none</a></div>
          <div class="desc">An attempt at defining the thing engineers admire and can't ship.</div>
        </div>
        <div class="tag">5 min</div>
      </div>
    </div>
  </section>

  <!-- ===== ELSEWHERE ===== -->
  <section class="r d7">
    <div class="sec-head">
      <h2>Elsewhere <span class="zh">别处</span></h2>
      <div class="meta">prefer email</div>
    </div>
    <div class="elsewhere">
      <a href="/cdn-cgi/l/email-protection#e38b8aa394828d849a968dcd85828d"><span class="k">Email</span><span class="v"><span class="__cf_email__" data-cfemail="2d45446d5a4c434a545843034b4c43">[email&#160;protected]</span></span></a>
      <a href="#"><span class="k">Twitter</span><span class="v">@wangyunfan</span></a>
      <a href="#"><span class="k">GitHub</span><span class="v">github.com/wangyunfan</span></a>
      <a href="#"><span class="k">LinkedIn</span><span class="v">in/wangyunfan</span></a>
      <a href="#"><span class="k">Substack</span><span class="v">wangyun.substack.com</span></a>
      <a href="#"><span class="k">Read.cv</span><span class="v">read.cv/wangyunfan</span></a>
    </div>
  </section>

  <!-- ===== FOOTER ===== -->
  <footer class="r d8">
    <div class="sig">
      <span class="mark">wangyun.fan · v3.0</span>
    </div>
    <div>
      <kbd>?</kbd> for shortcuts
    </div>
  </footer>

</main>

<script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script><script>
  // Status easter egg — click the dot to flip status
  (function(){
    const s = document.getElementById('status');
    if(!s) return;
    s.addEventListener('click', () => {
      const cur = s.getAttribute('data-flip') === '1';
      s.setAttribute('data-flip', cur ? '0' : '1');
    });
  })();

  // ? hint — show a tiny console message; keeps things minimal
  document.addEventListener('keydown', (e) => {
    if (e.key === '?'){
      console.log('%c hi.','color:#1d4ed8;font-family:monospace;font-size:14px');
      console.log('say hello → hi@wangyun.fan');
    }
  });
</script>

</body>
</html>
